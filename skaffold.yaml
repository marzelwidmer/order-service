apiVersion: skaffold/v2alpha3
kind: Config
metadata:
  name: order-service
build:
  tagPolicy:
    sha256: {}
  artifacts:
    - image: order-service
      jib: {}
  local:
      push: false

deploy:
  kubectl:
    manifests:
    - k8s/base/deployment.yaml
    - k8s/base/service.yaml
